<!DOCTYPE html>
<html>
  <head>
    <title>AnimauxFantastiques</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
  </head>

  <body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-black fixed-top px-3 py-3 mb-50">
      <div class="container-fluid">
        <%= link_to root_path do %>
          <%= image_tag "Logo.jpg", alt: "logo", style:"margin-right: 50px;height: 45px;"%>
        <%  end %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarText">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
               <%=  link_to "All animals", animals_path, class:"nav-link active", style:"height:30px"%>
            </li>
          </ul>
            <% if policy(Animal).create? %>
             <span class="btn btn-outline-light mx-3" style="font-size:15px">
             <%= link_to "Add an animal", new_animal_path%>
              </span>
            <% end %>
          <div>
            <% unless user_signed_in? %>
              <%= image_tag "placeholder_user.png", class:"avatar", id:"navbarDropdown",
              data: { toggle: "dropdown"}, aria:{ haspopup:true, expanded:false } %>
            <% else %>
              <% if current_user.photo != nil && current_user.photo.key != nil%>
                <%= cl_image_tag current_user.photo.key, class:"avatar", id:"navbarDropdown", aria:{ haspopup:true, expanded:false } %>
              <% else %>
                <%= image_tag "placeholder_user.png", class:"avatar", id:"navbarDropdown", aria:{ haspopup:true, expanded:false } %>
              <% end %>
            <% end %>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                <% unless user_signed_in? %>
                  <%= link_to "Sign in", new_user_session_path, class:"dropdown-item"%>
                <% else %>
                  <%= link_to "Bookings' history", bookings_path, class:"dropdown-item"%>
                  <%= link_to "Log out", destroy_user_session_path, method: :delete, class:"dropdown-item" %>
                <% end%>
            </div>
          </div>
        </div>
      </div>
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <div class="content-wrapper">
      <%= yield %>
    </div>
    <div class="footer">
      <div class="footer-links">
        <a href="#"><i class="fab fa-github"></i></a>
        <a href="#"><i class="fab fa-instagram"></i></a>
        <a href="#"><i class="fab fa-facebook"></i></a>
        <a href="#"><i class="fab fa-twitter"></i></a>
        <a href="#"><i class="fab fa-linkedin"></i></a>
      </div>
      <div class="footer-copyright">
        This footer is made with <i class="fas fa-heart"></i> at Le Wagon
      </div>
    </div>

  </body>
</html>
